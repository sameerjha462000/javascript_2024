/**
 * getElementById() method take the id of the element that you want to search.
 * It returns us the element, if found else we get null.
 */

// searches for the element whose id is 'bear-photo'
const img = document.getElementById("bear-photo");

// console.dir() method helps us to view the returned element in the object form.
console.dir(img);

const p = document.getElementById("main");

console.dir(p);
