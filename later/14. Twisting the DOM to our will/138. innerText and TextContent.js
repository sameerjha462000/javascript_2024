/**
 * innerText gives us all the things that would be displayed on the browser
 */

/**
 * textContent gives us all the text that is present inside the HTML element irrespective
 * of whether it is visible in the browser or not.
 */

const p = document.querySelector("#main");

p.innerText;
p.textContent;
